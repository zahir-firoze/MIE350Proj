package videoGame.model;

public class AccessoryList extends ProductList{
	//TODO change get method for Accessory
//	public Accessory get(int upcNum) throws NotFoundException {
//		if (!has(upcNum)) {
//			throw new NotFoundException("Cannot find product " + upcNum);
//		}
//		return (Product) map.get(new Integer(upcNum));
//	}

	//TODO change this method and give description with attributes for the Accessory class
//	public boolean insert(Student student) {
//
//		int stid = student.getId();
//		String stfname = student.getFirstName();
//		String stlname = student.getLastName();
//		String stemail = student.getEmail();
//
//		String qStr = "INSERT INTO Student Values ('" + stid + "', '" + stfname + "', '" + stlname + "', '" + stemail
//				+ "')";
//		boolean updateResult = Main.runUpdate(qStr);
//		return updateResult;
//	}
}
